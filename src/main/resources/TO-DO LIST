===> file controller <===

//all users
 1. add file (by all users) done
 2. get files (by all users)
 3. get user files "and their metadata"
 4. get file (using fileId) as base64

//admin & mod
 3. delete file (admins and mods)

 //mod
  4. get pending files (mod)
  5. accept file (mod)
  6. reject file (mod)